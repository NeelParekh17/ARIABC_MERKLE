I notice something important right away: this run's banner line is different from before —

```
--conn-fanout auto: raft-kafka=1, kafka-only=threads
```

That means someone already started addressing the fairness issue by making `--conn-fanout` auto-scale with thread count for `kafka-only` mode. But this run used `raft-kafka` mode, so `connFanout` stayed pinned at **1** the entire sweep. Let me verify against the actual script logic before listing issues.So this is confirmed: `bench_cluster_threads.sh` has been patched since the last version I viewed (`CONN_FANOUT` defaults to empty string and the label is just descriptive text), but **it never actually passes `--conn-fanout` to `run_4node_raft_cluster.sh` unless the user explicitly sets `--conn-fanout`** — line 150 only adds it to `COMMON_ARGS` if `CONN_FANOUT` is non-empty. Let's check what `run_4node_raft_cluster.sh` does when `--conn-fanout` isn't passed at all, and whether it has any "auto" logic of its own.This confirms it precisely. Now I have everything needed. Let me give you the full breakdown.

## Verdict: Still not a fair/clean comparison, but the gap has narrowed. Here's exactly what's going on and what's broken.

### What changed since last time (good)
Someone added auto-fanout logic: in `run_4node_raft_cluster.sh` line 418-419, if `--conn-fanout` isn't explicitly set and `ordering-mode=kafka-only`, it sets `CONN_FANOUT = NUM_TERMINALS` — i.e., terminals would actually get independent sockets in that mode. That's the right idea.

### Issue 1 — This run used `raft-kafka` mode, where fanout is hard-disabled
Line 426-431 of `run_4node_raft_cluster.sh`:
```
if [[ "$ORDERING_MODE" == "raft-kafka" && "$CONN_FANOUT" -gt 1" && "${ARIABC_ALLOW_RAFT_FANOUT:-0}" != "1" ]]; then
  echo "ERROR: --conn-fanout > 1 is disabled for raft-kafka deterministic runs."
```
So in `raft-kafka` mode (which is what this sweep ran — `Cluster ordering mode: raft-kafka`), fanout is **forced to stay at 1 no matter what**, by design. The log proves it: `connFanout=1` at every single thread count (1, 4, 8, 12), and `conn_calls=1` for every run. This is the same root issue as before — terminals 1→12 are all sharing one submission socket/reactor — just now it's an explicit, intentional restriction rather than an oversight.

**This is an architectural decision someone made, not a bug** — but it means the headline question ("is it fair to compare threads vs threads") is still **no**, for this mode. The fix is either:
- Implement real per-terminal parallelism safely under Raft ordering (the actual feature work), or
- Be explicit in all reporting that raft-kafka mode's "terminals" axis measures pipeline depth, not client parallelism, and stop sweeping it on the same x-axis as single-node thread counts.

### Issue 2 — The plateau is reproduced again, even worse than before
TPS: 1807 → 3555 → 4318 → 4310 (t=1,4,8,12). It saturates by t=8 and **never moves again at t=12**. `submit_attempts` stays ~82 across all four runs — the gateway is doing the same fixed amount of submission work regardless of terminal count, just with a deeper pipeline window behind it (`detWindow` = 256, 1024, 2048, 3072). That's pipeline depth scaling, not concurrency scaling.

### Issue 3 — `majority wait time` and `submit time` swap dominance, hinting at a second bottleneck
- t=1: submit=1705ms, majority_wait=9462ms (majority/Kafka-bound)
- t=4: submit=3974ms, majority_wait=2705ms
- t=8: submit=5343ms, majority_wait=2334ms
- t=12: submit=10267ms, majority_wait=886ms (submit-bound)

As terminals increase, the bottleneck visibly **shifts from Kafka-majority-wait to submit time** — submit time grows faster than wall time itself (10267ms of "submit time" inside a 4759ms wall-clock run, which only makes sense if submit time is summed/accumulated across overlapping async submits rather than wall-clock). This suggests the single shared reactor's submit path itself becomes the limiter as terminal count rises — consistent with one-reactor-many-lanes design, not added parallelism.

### Issue 4 — `--threads` semantics are still overloaded and not transparently reported
The CLI accepts `--threads` as an alias for `--num-terminals`, which then derives `detWindow` and `detPipelineDepth`. There's no separate, clearly-surfaced metric in the output for "actual concurrent submission paths" vs "logical pipeline lanes." Right now a reader of the sweep table sees `terminals: 1,4,8,12` and naturally assumes it's analogous to single-node `threads: 1,4,8,12` — that's the misleading part, independent of whether fanout is fixed by design.

### Issue 5 — `--skip-cluster-setup` wasn't actually honored
You passed `--skip-cluster-setup`, but the log still shows full server restarts, Raft cluster bring-up (`Phase 4`/`Phase 5`), and table restores on every single thread count (`postgres OK ... waiting for server to shut down... server started` repeated 4× per run). Looking at `bench_cluster_threads.sh`, `--skip-cluster-setup` only adds `--skip-sync --skip-build --skip-kafka --skip-rdkafka-setup` — it does **not** skip the server restart/Raft-bringup/table-restore phases inside `run_4node_raft_cluster.sh`. That's not a correctness bug for the TPS numbers themselves (state is reset cleanly each time, which is good for reproducibility), but it means `--skip-cluster-setup` is mislabeled/incomplete — it doesn't actually skip "cluster setup," just package installation steps. Worth fixing the flag semantics or renaming it.

---

### Summary list to hand to Sonnet for fixes, ranked by impact:

1. **Architectural**: `raft-kafka` mode hard-disables `connFanout > 1`. Either implement safe multi-socket submission under Raft ordering (the real fix, likely needs ordering-path-aware multiplexing rather than one shared reactor), or explicitly fence off raft-kafka "terminals" sweeps from being plotted against single-node "threads" sweeps.
2. **Measurement/bottleneck**: `submit_attempts` and `conn_calls` are flat across all terminal counts in raft-kafka mode — the reactor isn't actually increasing concurrent work as terminals grow past 8; only pipeline depth grows. Need a counter that's terminal-count-sensitive (e.g., per-lane submit count) to confirm/refute this directly instead of inferring it.
3. **Diagnostic**: `submit time (ms)` exceeds wall-clock time at higher terminal counts (10267ms inside a 4759ms run) — this metric's accumulation logic (sum across async ops vs wall-clock) needs clarifying/fixing so it's actually interpretable.
4. **CLI/UX**: `--skip-cluster-setup` doesn't skip cluster setup (server restart, Raft bring-up, table restore still run every iteration) — rename or extend the flag to match its name.
5. **Reporting**: `--threads`/`--num-terminals` should be clearly distinguished from "concurrent submission paths" in all printed summaries, so the sweep table doesn't visually imply the same kind of scaling as the single-node thread sweep.