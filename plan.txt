Verdict: **much better than before, but still not fully apples-to-apples if you claim “same clients.”**

You fixed the **big old problem**: full-system `x=1,2,4...` is no longer just a fake label for `detWindow`. Now the full system actually accepts real `--numTerminals > 1`, keeps one global deterministic order, and uses `thread = numTerminals`. That is a real improvement. The uploaded log says the new gateway accepts real `numTerminals`, uses terminal lanes with bounded `detPipelineDepth`, and the runner now uses `FULL_THREAD_KNOB=client-pipeline`, `FULL_DET_PIPELINE_DEPTH=80`, `FULL_DET_BATCH_SIZE=80`. 

But the remaining issue is this:

```text
Single-node side = normal client threads / likely blocking benchmark threads
Full-system side = real numTerminals, but each terminal has pipeline depth 80
```

So the x-axis is now **less fake**, but the client behavior is still not identical.

### What is fine now

This part is good:

```text
thread=1  -> full numTerminals=1, detPipelineDepth=80, detWindow=80
thread=2  -> full numTerminals=2, detPipelineDepth=80, detWindow=160
thread=4  -> full numTerminals=4, detPipelineDepth=80, detWindow=320
...
```

That is a clean rule. No weird x=2 special override. No hidden per-point tuning goblin hiding under the graph rug.

Also correctness looks good:

```text
Full-system runs: 21/21 valid
Single-node runs: 42/42 valid
completion_path = kafka_majority
divergence_count = 0
permanent_failures = 0
Merkle verification passed
```

That means the full-system numbers are not just fast, they are going through the intended Kafka-majority correctness path. 

The batch-fill bug fix is also important. Earlier, the gateway filled the slots once and then kept topping up one request at a time, creating around 20k submit frames and only around 364 TPS. After forcing full declared batches, t1 reached the 4k range. That fix is conceptually correct because `detBatchSize=80` should actually produce batched submit behavior, not a drizzle of single-request Raft frames. 

### What is still an issue

The biggest remaining issue is **single-node baseline mismatch**.

Your final result says:

| thread | single DET | full system | full vs DET |
| -----: | ---------: | ----------: | ----------: |
|      1 |    4215.45 |     4375.06 |      +3.79% |
|      2 |    6138.02 |     7575.56 |     +23.42% |
|      4 |    8630.60 |     8130.58 |      -5.79% |
|      6 |    9623.62 |     9641.83 |      +0.19% |
|      8 |   10769.36 |    10625.83 |      -1.33% |
|     10 |   10683.21 |    10175.19 |      -4.76% |
|     12 |   10823.45 |     9997.05 |      -7.64% |

The t2 point is the red flag. Full-system being **+23.42% faster than single-node DET** is not impossible, but it strongly suggests the comparison is no longer “same client behavior.” Your uploaded note itself says the caveat: t2 is faster because the full-system now uses explicit terminal pipelining, while the existing single-node baseline is blocking-client style. 

So the honest interpretation is:

```text
This is a valid full-system pipelined-throughput experiment.
It is not yet a perfectly apples-to-apples overhead comparison against single-node DET.
```

### How to make it fully proper

You need to run the **single-node side with the same client-pipeline model**.

That means:

```text
Single-node DET:
  numTerminals = x
  detPipelineDepth = 80
  detBatchSize/client batch behavior = 80 or equivalent
  same total tx count
  same workload
  same 3/5 runs

Full-system:
  numTerminals = x
  detPipelineDepth = 80
  detBatchSize = 80
  Kafka-majority path
```

Then the graph title can honestly be:

```text
TPS vs Real Client Terminals, Pipeline Depth = 80
```

Right now, the safer title is:

```text
Single-node blocking DET vs full-system pipelined Kafka-majority throughput
```

That title is honest, but it is not as strong.

### One more statistical issue

You should do **5 runs**, not just 3, for the final graph. In the zip summary, t10 has a big clean outlier: one run is around 6.8k TPS while the other two are around 10.1k TPS. With only 3 runs, a trimmed mean is a bit of a magic wand. Use 5 runs and show median or error bars. Tiny benchmark dragons love hiding in 3-run results.

### Final answer

So, is it fine now?

**For engineering validation: yes, it is good.**
You proved the full system can do 4k+ TPS with real `numTerminals`, terminal lanes, batching, Kafka-majority validation, and clean Merkle checks.

**For a strict apples-to-apples claim: still not fully.**
The remaining problem is that the full-system clients are pipelined with depth 80, while the single-node baseline appears to be ordinary blocking client threads. To make it fully defensible, rerun single-node DET with the same pipeline depth and same terminal model.

Best final claim right now:

```text
With real full-system terminals and bounded per-terminal pipelining, the Raft+Kafka system reaches throughput close to the single-node deterministic baseline while passing Kafka-majority and Merkle consistency checks. However, the current graph should be treated as a pipelined full-system throughput comparison, not a strict blocking-client apples-to-apples overhead comparison.
```
