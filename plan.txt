Add Kafka (multi-node, pre-serialized input)
Now bring in multiple replicas but feed them a pre-serialized transaction log (bypass Raft for now). Kafka carries results back.
Correctness check:

All replicas must return the same result hash for every transaction. Feed a compareStates manually and confirm Merkle roots match across all replicas.
Deliberately corrupt one replica's row, run compareStates, verify the divergence is detected.

Performance check: throughput will drop compared to single node (Kafka messaging overhead). This is expected. The number you get here is your Kafka-only overhead baseline.